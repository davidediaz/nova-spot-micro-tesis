# PDF de conexiones

Compilar desde esta carpeta:

```bash
pdflatex -interaction=nonstopmode -halt-on-error main.tex
pdflatex -interaction=nonstopmode -halt-on-error main.tex
```

El PDF distingue cableado confirmado, propuestas pendientes y conexiones que no
deben energizarse todavía.
